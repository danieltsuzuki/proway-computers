import { DescricaoLengthPipe } from './descricao-length.pipe';

describe('DescricaoLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new DescricaoLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
